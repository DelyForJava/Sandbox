require 'Global'

--require 'Hall/Hall'
--require 'Game/Game'


local currentScene
local index=0

function Awake()
		print("-------------------------------- Awake:"..CS.Bean.Hall.AssetBundleManager.Instance.Version.." --------------------------------")
		currentScene = require 'Login/Login'

		USceneManager.LoadScene(currentScene.name);
end

function Start()
		--print("-------------------------------- Start --------------------------------")
		currentScene.Start()
end

function Update()
		-- print("-------------------------------- Update --------------------------------")
		currentScene.Update()
end

function OnDestroy()
		-- print("-------------------------------- OnDestroy --------------------------------")
		currentScene.OnDestroy()
end